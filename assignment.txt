1.Are the HTML tags and elements the same thing?

Ans:- No the html tag begin with < symbol and ending with the symbol >. whatever written inside the symbol are called HTML tags.
      & HTML elements are those which consist some structure or expression its generally contain start tag , content and end tag.

2.What are tags and attributes in HTML?

Ans:-The html tag begin with < symbol and ending with the symbol >. whatever written inside the symbol are called HTML tags.
     eg.[<p> </p>& HTMl attributes used to define the character of an HTML element. It always placed in the opening tag of 
     an element. It generally provides additional styling to the element.eg.[<p align="center"> hello </p>]

3.What are void elements in HTML?

Ans:-There is a special group of elements that only have start tags and does not contain any content within it, these elements are 
     called void elements. Void elements doesn’t have ending tags and can only have attributes but do not contain any kind of content.
     Eg. <BR> <HR> etc.

4.What are different types of lists in HTML?

Ans:- There are three types of list 1.Orderlist 2.Unorderlist and 3.Defination list .Order list use to make a list in specific 
     sequance. Unorder list does not make a sequance list and Defination list define Terms and Defination.

5.What is class attribute in HTML?
Ans:-The class attribute specifies one or more classnames for an element.The class attribute is mostly used to point to a class
     in a style sheet. 

6.what is different between the 'id' attribute and the 'class' attribute of HTML elements?
Ans:-The only difference between them is that “id” is unique in a page and can only apply to at most one element, while “class” 
     selector can apply to multiple elements. 

7.What are the various formatting tags in HTML?
Ans:- Formatting elements were designed to display special types of text are as follows:<B>Bold, <Strong>important text, <i>italic,
      <em>emphasized, <mark>marked text, <ins>inserted, <del>deleted, <small>smaller text, <code>codes, <sub>subscript, <sup>superscript.

8.How is cellpadding different from cell spacing?
Ans:-Cellpadding basically defines the space present between a table cells border and the content present in it. Cellspacing 
     basically defines the space present between individual adjacent cells.

9.How can we club two or more rows or colunms into a single row or columns in an HTML table?
Ans:- To club two or more rows we can use rowspan attribute and to club two or more columns we can use colspan attribute in <td> tag.

10.What is the difference between a block-level element and an inline element?
Ans:-Block elements always start from a new lineA block-level element always takes up the full width available.Two commonly used 
     block elements are <p> and <div>. Inline elements never start from a new line.An inline element only takes up as much width
     as necessary eg.Span tag.

11.How to create a hyperlink in HTML?
Ans:- The hyperlink can create by <a> tag .If we put URL in <a href=""> and give the name to this that word will work like a link.

12.What is the use of an iframe tag?
Ans:-The iframe tag specifies an inline frame.An inline frame is used to embed another document within the current HTML document.

13.What is the use of span tag? Explain with example.
Ans.The span tag is an inline container used to mark up a part of a text, or a part of a document.The span tag is easily styled by CSS
    eg. <p>Good morning<span>have a great day</span></p>
        <Style> span{background-color:yellow;}

14.How to insert a picture into a background image of web page?
Ans:-To set the background image of a webpage. Under the CSS style tag, add the property background-image.
     The property sets a graphic such as jpg, jpeg, gif, etc.

15.How are active links different from normal links?
Ans:-A normal link is just a line of code that contains a pointer to another resource. An active link is that line of code in action, 
     opening that other resource.

16.What are the different tags to separate sections of text?
Ans:-There are basically three types of tag <br>, <p> & <blockquote>.BR tag is use to break a line of text. P tag is use for new 
     paragraph and BLOCKQUOTE is use for quoted section.

